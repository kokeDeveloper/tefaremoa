# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Environment variables for local development
# MySQL connection string format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# If your root user has no password, use: mysql://root@localhost:3306/tfDb
# If it has a password, use: mysql://root:YourPassword@localhost:3306/tfDb

DATABASE_URL="mysql://koke:cocacola@149.50.134.135:3306/tfDb"
PORT=3000

# Development JWT secret (override in production via .env.local)
JWT_SECRET="dev-secret"
NEXT_PUBLIC_SSO_URL=http://localhost:3000/api/auth/sso
NEXT_PUBLIC_FORCE_SSO=1
FORCE_SSO=1
